version: "3.1"

intents:
  - greet
  - url

actions:
 - action_echo_msg
 - image_text

entities:
 - image_url

responses:
  utter_greet:
  - text: "hi"


session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
